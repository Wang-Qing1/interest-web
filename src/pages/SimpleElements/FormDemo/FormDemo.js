/**
 * 表单示例
 */
import './FormDemo.css';
import React from "react";
import Base from "../../Base/Base";

export default class FormDemo extends Base {
  renderContent() {
    return (
      <div>
        表单示例
      </div>
    );
  }
}